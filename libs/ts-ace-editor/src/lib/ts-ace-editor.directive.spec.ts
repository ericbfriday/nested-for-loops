import { TsAceEditorDirective } from './ts-ace-editor.directive';

describe('TsAceEditorDirective', () => {
  it('should create an instance', () => {
    const directive = new TsAceEditorDirective();
    expect(directive).toBeTruthy();
  });
});
